{
  "name": "python-app-with-electron-gui",
  "version": "1.0.0",
  "description": "A minimal Electron application",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . PetFinder --out PetFinder --overwrite --platform=win32 --arch=x64 --icon=dog.ico --prune=true --squirrel-install --ignore=node_modules/electron-* --electron-version=1.7.9 --ignore=PetFinder-win32-x64 --version-string.CompanyName=AninHuang --version-string.ProductName=PetFinder"
  },
  "license": "CC0-1.0",
  "devDependencies": {
    "electron": "^11.5.0",
    "electron-packager": "^14.0.6"
  },
  "dependencies": {
    "masonry-layout": "^4.2.2",
    "python-shell": "^1.0.7"
  }
}
